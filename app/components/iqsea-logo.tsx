export function IQseaLogoNav({ className = "h-9" }: { className?: string }) {
  // eslint-disable-next-line @next/next/no-img-element
  return <img src="/brand/logo-white-tagline.svg" alt="IQSEA" className={className} style={{ width: "auto" }} />;
}

export function IQseaLogo({ className = "h-8" }: { className?: string }) {
  // eslint-disable-next-line @next/next/no-img-element
  return <img src="/brand/logo-white-compact.svg" alt="IQSEA" className={className} style={{ width: "auto" }} />;
}

export function IQseaLogoSmall({ className = "h-6" }: { className?: string }) {
  // eslint-disable-next-line @next/next/no-img-element
  return <img src="/brand/logo-white-compact.svg" alt="IQSEA" className={className} style={{ width: "auto" }} />;
}
